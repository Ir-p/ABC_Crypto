<!-- homepage-root the landing page-->
<hr>
<div class="container-fluid">
  <div class="row">
    <div class="col">
      <div class="card m-0">
        <div class="card-header">
          <h5 class="card-title">{{card.name}}</h5>
        </div>
        <div class="card-body">
          <h6 class="card-body">{{card.content}}</h6>
        </div>
      </div>
    </div>
  </div>
</div>