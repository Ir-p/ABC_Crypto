<!-- edit-comment -->
<div class="col">
  <div class="card">
    <div class="card-header">
      <div class="fs-3 fw-bold">Edit Comment</div>
    </div>

    <div class="card-body">
      <div class="fw-bold">{{link.name}} </div>
      <div><a href="{{link.link}}">{{link.link}}</a></div>
      <div>{{user.first_name}} {{user.last_name}} on {{format_date date_created}}</div>
      <form class="form edit-comment-form">
        <div class="form-group mb-3">
          <label for="edit-comment-content" class="form-label fw-bold">Content</label>
          <textarea class="form-control" id="edit-comment-content" rows="5">{{comment}}</textarea>
        </div>
        <div class="form-group mb-3">
          <button class="btn btn-primary" type="submit" id="update-comment-btn">Update</button>
          <button class="btn btn-primary" type="submit" id="delete-comment-btn">Delete</button>
          <button class="btn btn-primary" type="submit" id="cancel-comment-btn">Cancel</button>
        </div>
      </form>
    </div>
  </div>
</div>

<script src="/js/edit-comment.js"></script>
<script src="/js/cancel.js"></script>