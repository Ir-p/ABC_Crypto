<!-- create-post -->
<div class="col">
  <div class="card m-2">
    <div class="card-header bg-light">
      <div class="fs-3 fw-bold">Create Post</div>
    </div>
    <div class="card-body">
      <form class="form post-form">
        {{!-- <input type="hidden" value="{{user_id}}" name="user_id" class="form-control" id="user_id"> --}}
        <div class="form-group mb-3">
          <label for="create-post-title" class="form-label fw-bold">Select a Link</label>
          <select class="form-select" id="create-post-link-id">
            {{#each links}}
              <option value="{{id}}">{{name}}</option>
            {{/each}}
          </select>
        </div>
        <div class="form-group mb-3">
          <label for="create-post-content" class="form-label fw-bold">Comment</label>
          <textarea class="form-control" id="create-post-content" rows="5"></textarea>
        </div>
        <div class="form-group mb-3">
          <button class="btn btn-primary" type="submit" id="create-post-btn">Create</button>
          <button class="btn btn-primary" type="submit" id="cancel-post-btn">Cancel</button>
        </div>
      </form>
    </div>
  </div>
</div>

<script src="/js/new-post.js"></script>
<script src="/js/cancel.js"></script>